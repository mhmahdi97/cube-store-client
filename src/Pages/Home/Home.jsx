import React from 'react';
import useTitle from '../../hooks/useTitle';

const Home = () => {

    useTitle('Cube Store | Home')

    return (
        <div>
            
        </div>
    );
};

export default Home;